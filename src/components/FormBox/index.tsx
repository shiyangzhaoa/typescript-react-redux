import * as React from 'react';
import './index.scss';

interface Props {
  user: object;
}

const FormBox = (props: Props) => (
  <div className="form-box">
    <button>nnn</button>
  </div>
);

export default FormBox;